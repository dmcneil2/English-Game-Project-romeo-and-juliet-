///change_sprites(sprite_index, animation_speed, image_index)
var spr = argument[0];
var spd = argument[1];
var idx = argument[1];
if (sprite_index != spr) {
    sprite_index = spr;
    image_index = idx;
    image_speed = spd;
}
